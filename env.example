# Supabase Konfiguration
# Erstellen Sie ein Supabase-Projekt unter https://supabase.com
# und kopieren Sie die URL und den anonymen Schlüssel hier ein

PUBLIC_SUPABASE_URL=your_supabase_project_url
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Beispiel für die Datenbankstruktur:
# 
# Tabelle: products
# - id (int, primary key)
# - name (text)
# - description (text)
# - stock (int)
# - min_stock (int)
# - category (text)
# - marke (text)
# - sell_price (decimal)
# - created_at (timestamp)
# - updated_at (timestamp)
#
# Tabelle: access_codes
# - id (int, primary key)
# - code (text, unique, Format: XXXX-XXXXX-XXXXX-XXXX)
# - is_active (boolean)
# - created_at (timestamp)
#
# Tabelle: user_accounts (optional)
# - id (int, primary key)
# - username (text)
# - password_hash (text)
# - access_code_id (int, foreign key)
# - created_at (timestamp)
# - updated_at (timestamp) 